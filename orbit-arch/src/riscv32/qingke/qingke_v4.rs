pub struct Core {
    // csr: CSR,
    // csr_ext: CSR_Ext,
}

impl Core {
    pub const fn new() -> Self {
        Self {
            // csr: CSR::new(),
            // csr_ext: CSR_Ext::new(),
        }
    }
}
