[alias]
bin = "build -p orbit-bin --release --features"
bin-size = "size -p orbit-bin --release -q --features"
bin-objdump = "objdump -p orbit-bin --release -q --features"

[build]
target = "riscv32imc-unknown-none-elf"

[unstable]
build-std = ["alloc", "core"]
