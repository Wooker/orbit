[package]
name = "orbit-bin"
version = "0.1.0"
edition = "2024"

[dependencies]
orbit-arch = { path = "../orbit-arch", features = [ "qingke_v4"] }
orbit-kernel = { path = "../orbit-kernel", features = [ "ch592"] }
orbit-app = { path = "../orbit-app" }

[features]
default = [ "ch592" ]
ch592 = []
