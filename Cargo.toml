[workspace]
resolver = "2"

members = [
    "orbit-arch",
    "chip",
    "orbit-kernel",
    # "orbit-libos",
    # "orbit-app",
    "orbit-bin", "orbit-common", "orbit-common-proc-macro",
]
exclude = ["orbit-apps"]
